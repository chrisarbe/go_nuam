name: betterscan

on: push

jobs:
    betterscan:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repository
              uses: actions/checkout@v4
            - run: |
                echo "Checking out repository"
                ls -la
            - name: Betterscan Scan
              run: |
                echo "Running Betterscan"
                curl https://dl.betterscan.io/cli-html.sh
                ./cli-html.sh
                ls -la